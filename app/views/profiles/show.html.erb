<h2 align="center">
  Title: <%= @profile.title %>
</h2>
<div class = "card bg-secondary col-lg-8 offset-lg-2">
  <ul class= "listing">
    <div class="row center">
      <div class = "col-lg-4 offset-lg-4">
        <li>Created By:</li>
        <li><%= link_to gravatar_for(@profile.user), user_path(@profile.user) %></li>
        <li class="article-title">
          <%= link_to @profile.user.username, user_path(@profile.user) %>
        </li>
        <li>
          <small><%= pluralize(@profile.user.profiles.count, "profile") if @profile.user.profiles%></small>
        </li>
      </div>
      </div>
    <div>
  </ul>
  <h4 class="center description"><strong>Description:</strong></h4>
  <hr>
  <%= simple_format (@profile.description) %>
  <div class = "profile-actions">
    <%= link_to "View All Profiles", profiles_path, class: "btn btn-xs btn-success" %>
     <%= link_to "Delete Profile", profile_path(@profile), method: :delete, 
                  data: {confirm: "Are you sure you want to delete this article"}, 
                  class: "btn btn-xs btn-danger" %>
    <%= link_to "Edit Profile", edit_profile_path(@profile), class: "btn btn-xs btn-primary" %>
  </div>
</div>